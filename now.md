# Current Status

- The multi-subscription / routing work is implemented in the feature branch.
- The PR has been prepared and marked ready.
- CI has been run; the latest rerun still needs a final check before merge if it has not already completed.
- The local daemon is restored and healthy again.
- The running daemon is currently on the verified local build and reports `0.1.22`.

# Next Step

- If you want the newer release asset specifically, that needs a separate investigation because the downloaded `v0.1.23` binary was being killed on launch on this machine.
